let first = "Hel";
let end = "orld !!";
console.log(`${first}lo, w${end}`);

function animalCan() {
  birdCan();
  for (a of Object.entries(animal)) {
    console.log(a[0] + ": " + a[1]);
  }
}
birdCan();
